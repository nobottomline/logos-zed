{
  "name": "tree-sitter-logos-zed",
  "version": "0.1.0",
  "description": "Tree-sitter grammar and Zed extension for Theos Logos syntax highlighting.",
  "license": "MIT",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "generate": "tree-sitter generate",
    "test": "tree-sitter test",
    "parse": "tree-sitter parse --quiet examples/*.x examples/*.xm examples/*.xi examples/*.xmi"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.24.7",
    "tree-sitter-objc": "^3.0.2"
  },
  "tree-sitter": [
    {
      "scope": "source.logos",
      "file-types": [
        "x",
        "xm",
        "xi",
        "xmi"
      ]
    }
  ]
}
